@model KisanKiDukan.Models.ViewModels.CategoryModel
@{
    ViewBag.Title = "Add";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
@*<h2>Add </h2>*@
@using (Html.BeginForm("Add", "Category", FormMethod.Post, new { encType = "multipart/form-data" }))
{
    <div style="width:50%">
        <h4>Add Categories</h4>
        <div class="form-group">
            Name * @Html.TextBoxFor(a => a.CategoryName, new { @class = "form-control" })
            <span style="color:red">@Html.ValidationMessageFor(a => a.CategoryName)</span>
        </div>
        <div class="form-group">
            Image
            @Html.TextBoxFor(a => a.ImageFile, new { type = "file" })
        </div>
       <div class="form-group">
            @if (TempData["msg"] != null)
            {
                <span style="color:red;">@TempData["msg"]</span>
            }
        </div>
        <div class="form-group">
            <input type="submit" value="Submit" class="btn btn-info" />
            <a class="btn btn-info" href="/Category/Categories">Show All</a>
        </div>
    </div>
}
