@model KisanKiDukan.Models.ViewModels.CatWithProdVm

@*<ul class="level-2">
    @if (Model != null && Model.Cateogries.Count() > 0)
    {
        <li class="item-2 " style="width:100%">
            <div class="menu-content">
                <div class="cha clearfix">
                    <ul>
                        @foreach (var item in Model.Cateogries)
                        {
                            <li style="width:20%;float:left;padding:0 15px;">
                                <img src="/Images/@item.CategoryImage" /><h5>
                                    <a href="/Home/Products?categoryId=@item.CatId">@item.CategoryName</a>
                                </h5>
                            </li>
                        }

                    </ul>
                </div>
            </div>
        </li>
        foreach (var item in Model.Cateogries)
        {
            <li class="item-2  parent" style="width:20%">
                <a href="#" title="">@item.CategoryName</a>
                <div class="dropdown-menu">
                    @if (item.Products != null && item.Products.Count() > 0)
                    {
                        <ul>
                            @foreach (var p in item.Products)
                            {
                            <li class="">
                                <a href="/Home/ProductDetail?productId=@p.ProdId" title="@p.ProdName">@p.ProdName</a>
                            </li>
                            }

                        </ul>
                    }
                </div>
                <span class="grower close"> </span>
            </li>

        }
    }
</ul>*@
